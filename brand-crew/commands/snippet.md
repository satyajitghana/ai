---
description: Add a code snippet (CUDA/PyTorch/C++/bash).
argument-hint: [title or code]
---

Use the **add-snippet** skill for: $ARGUMENTS

Write `content/snippets/<slug>.mdx` with `lang` set and a fenced code block, then run `pnpm validate`. Ship via PR.
