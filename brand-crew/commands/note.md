---
description: Add or grow a digital-garden note.
argument-hint: [title | concept]
---

Use the **new-note** skill for: $ARGUMENTS

Write/edit `content/notes/<slug>.mdx`, interlink with `[[wikilinks]]`, bump `updated` when revising, then run `pnpm validate`. Ship via PR.
